# 高斯实景查看平台 技术方案（v1）

- 版本：v1（2026-09-05）

- 适用范围：基于 `@playcanvas/supersplat-viewer` 的 Web 高斯查看平台（现状工程：`D:\lm\w`）

- 配套数据：`data/` 下的"套盒"——高斯点云（PLY）+ 配套 GeoJSON +（可选）碰撞网格

***

## 0. 决策摘要（先看结论）

| #  | 问题                                     | 结论                                                                                                                                   |
| -- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| D1 | 跨端（PC / 安卓 / 鸿蒙 / 苹果）是否直接用 PLY，不转 SOG？ | **直接用 compressed PLY，不转 SOG**。viewer 原生支持、无切片/转换环节；标签与碰撞均为自研叠加层，SOG 无增量收益。                                                           |
| D2 | 空气墙用 GeoJSON 转 JSON，还是直接提供 OBJ/mesh？   | **两条路径统一走"3D 网格 → 体素化"管线**。边界级空气墙可用 GeoJSON 自动挤出生成；室内/门洞级碰撞推荐数据方直接提供 OBJ/GLB 网格。GeoJSON 本身就是 JSON，不能"改名即当碰撞"，中间必须历经"2D→3D 挤出→体素化"两步。 |
| D3 | 在高斯上做类似 three.js CSS3D 的交互标签？          | **新增 three CSS3DRenderer 叠加层**（DOM 标签），与现有 WebGL 叠加层、viewer 画布形成三层结构，共享同一相机；点击/悬停做交互触发。                                              |

***

## 1. 背景与目标

### 1.1 现状

工作区 `D:\lm\w` 已搭建最小可运行的高斯查看器：

- 加载：`@playcanvas/supersplat-viewer 1.28.0`，默认加载 `data/point_wlbwg.compressed.ply`（147.3 MB，9,126,581 splat，PlayCanvas compressed PLY）。

- 服务：`scripts/serve.mjs` 把 `/data/*` 零拷贝映射到根目录 `data/`，支持 Range 请求。

- 碰撞：`data/point_wlbwg.voxel.json(+.bin)`（稀疏八叉树）+ `point_wlbwg.collision.glb`（可视化网格），第一人称 walk/fly 模式不穿模。

- 叠加层：`src/overlay.js` 在 viewer 画布上再叠一层透明 three.js WebGL canvas，把 `data/lx.geojson` 的 2 个多边形按 z 均值基准面挤出为半透明青色边界体，并每帧同步 viewer 相机。

### 1.2 数据套盒（数据生产方交付形态）

`data/` 下同套存放：

| 文件                                          | 说明                           |
| ------------------------------------------- | ---------------------------- |
| `point_wlbwg.compressed.ply`                | 高斯点云（912 万 splat）            |
| `lx.geojson`                                | 配套边界/区域矢量（2 个 Polygon），同套坐标系 |
| `point_wlbwg.collision.glb` / `.voxel.json` | 当前场景的碰撞体（现方案产物，非原始交付物）       |

- 坐标系约定：GeoJSON 顶点坐标与 PLY 为**同一套本地直角坐标、单位米**（文件头 `CRS84` 为导出工具占位，实为本地坐标）；GeoJSON 顶点第 3 维 z = 该边界的高程。

- 坐标到世界空间的变换：viewer 默认将 splat 实体绕 X 轴 -90°（Z-up → Y-up），叠加层对应关系为：

```
数据坐标 (px, py, pz)  →  世界坐标 (px, pz, -py) [+ 基准高度偏移]
```

（见 `src/overlay.js` loadPolygons / buildExtrudedMesh，已与点云验证对齐。）

***

## 2. D1 数据格式决策：PLY 直用，不做 SOG

### 2.1 跨端平台能力矩阵

| 端                              | 内核       | WebGPU     | WebGL2 | 数据档位建议  | 说明               |
| ------------------------------ | -------- | ---------- | ------ | ------- | ---------------- |
| Windows（Chrome/Edge）           | Blink    | 原生支持       | 支持     | 全量 9M   | 首选 WebGPU        |
| macOS（Safari）                  | WebKit   | 新版实验性      | 支持     | 全量或降档   | 兜底 WebGL2        |
| Android（Chrome / WebView）      | Blink    | 部分设备可用、不稳定 | 支持     | 2–3M 分档 | 兜底 WebGL2        |
| 鸿蒙（ArkWeb / WebView）           | Chromium | 需真机实测      | 支持     | 2–3M 分档 | 策略同 Android，真机验证 |
| iOS / iPadOS（Safari / WebView） | WebKit   | 仅新版实验性     | 支持     | 2–3M 分档 | 兜底 WebGL2        |

**结论：统一以 WebGL2 为兼容兜底（`?webgl=1`），全端确实"用 PLY 就行"。**

### 2.2 compressed PLY vs SOG（.sog）

| 维度                  | compressed PLY（现状）       | SOG（SuperSplat OpenGauss）      |
| ------------------- | ------------------------ | ------------------------------ |
| 本质                  | 单个 splat 数据文件（packed 属性） | 场景归档：JSON 场景图 + 若干数据文件（可 gzip） |
| viewer 加载           | 原生，官方默认                  | 原生                             |
| 转换成本                | 无                        | 需生成器/编辑器导出，多一道工序               |
| 多对象打包/变换            | 无                        | 支持（多 splat + transform）        |
| 内嵌 annotations/编辑数据 | 无                        | 支持                             |
| 对 GPU 渲染的 splat 数   | 不影响                      | 不影响                            |
| 网络体积                | 已按 vertex 打包压缩           | gzip 后可略小，但**不改变渲染负载**         |

**为什么不转 SOG：**

1. 单对象场景，SOG 的"多对象打包/变换/内嵌标注"用不上；
2. 标签（D3）与碰撞（D2）都在 viewer 之外自研实现，不依赖 SOG 场景图；
3. 转换 + 版本兼容（.sog 结构随编辑器升级变动）是纯增量麻烦；
4. 移动端卡顿的根因是 912 万 splat 的 GPU/内存负载，SOG 解决不了——要靠降档数据与预算控制。

> 何时才值得切 SOG：未来出现"多个 splat 对象带各自变换一次性分发"或"需用官方编辑器内嵌 annotations 后直接给 viewer 消费"时，再评估。届时标签方案同步改为官方 annotations 体系。

### 2.3 性能与加载策略（不切片）

- **不切瓦片/不做格式切片**：当前单场景规模（9M）用单文件 + 分档即可；城市场景多份套盒时按文件夹组织，各加载各的。

- **Range 分块流式**：compressed PLY 在支持 Range/206 的服务器上可渐进拉取，首屏先出低分辨率；本地 `serve.mjs` 已支持，生产静态服务器（nginx 等）默认支持。

- **移动端分档**：用 splat-transform 降采样另存一档（如 300 万点）`data/point_wlbwg.lite.compressed.ply`，按设备档位参数（`?tier=pc|mobile` 或 UA 自动）选择；配合 `?budget=` 双保险。

- **备选瘦身**：若移动端带宽仍是瓶颈，可评估 `.spz`（更小）——但需增加转换环节，与"减少切片麻烦"目标冲突，**仅当带宽实测不合格时再启用**。

***

## 3. D2 空气墙决策：GeoJSON 还是 OBJ/mesh

### 3.1 先分清两个概念

| 概念        | 现状实现                                                    | 作用         |
| --------- | ------------------------------------------------------- | ---------- |
| 可视化边界     | `overlay.js` GeoJSON 挤出体（半透明青色网格，`?poly=0` 可隐藏）         | 给你看的红线/区域  |
| 物理碰撞（空气墙） | viewer 原生碰撞：`voxel.json(+.bin)` 八叉树；`collision.glb` 可视化 | 相机走不出去、不穿模 |

**GeoJSON 是 2D 边界数据**（本套盒顶点带 z 也只是'边界沿线高程'）。要当空气墙，必须补足第三维语义：墙的高度/厚度、门洞、楼板、天花板——纯 GeoJSON 一项都给不了。因此：

- 只靠 GeoJSON → 能生成"场地边界/禁入围合"级空气墙（封闭筒体）。

- 想要"房间级、有门洞、分层"的室内碰撞 → 必须由数据方提供真实 3D 网格（**OBJ 或优先 GLB**）。

### 3.2 统一空气墙管线（两条路径汇成一条）

```
路径 2a（仅有 GeoJSON 时）          路径 2b（推荐：数据方给 3D 网格）
GeoJSON Polygon 环                     OBJ / GLB(优先) 封闭网格
        │ 挤出（顶面+底面+四周侧壁）          │
        └───────────┬──────────────────────┘
                    ▼
          统一 3D 网格（GLB）
                    │ 体素化（splat-transform）
                    ▼
   data/xxx.voxel.json(+.bin)  +  xxx.collision.glb
                    │
              viewer 默认 collisionUrl 指向
```

- 生成命令（以现有场景为模板；输入换成新网格/点云，`--seed-pos` 换成室内空腔内的点）：

```
splat-transform data/point_wlbwg.compressed.ply -r 90,0,0 --voxel-size 0.5 --voxel-external-fill --seed-pos 0,2,25 --collision-mesh faces -g 1 -w data/point_wlbwg.voxel.json
```

- `运行 viewer` 默认已指到该文件（`build.mjs` H6 补丁），换套盒时用 `?collision=./data/xxx.voxel.json` 覆盖或重新构建。

- **对齐校验**：`?coldbg=1` 打开碰撞高亮，逐面核对与画面贴合。

### 3.3 直接回答："GeoLSON 转 JSON 建空气墙，还是直接给 OBJ/mesh？"

1. **GeoJSON 不用"转 JSON"**——它本身就是 JSON；viewer 的碰撞体 `voxel.json` 也是 JSON。但两者天差地别：`voxel.json` 是稀疏八叉树体素描述，GeoJSON 是 2D 矢量环。**把 geojson 改扩展名不会产生碰撞能力**，必须执行"2D→3D 挤出 → 体素化"两步。
2. **推荐交付形态**：数据方直接提供 **GLB（首选，二进制、可带材质/normal）/ OBJ（可接受）** 的封闭碰撞网格。理由：信息最全（墙体厚度、门洞、分层都在模型里），管线最短，误差最小。
3. **边界级空气墙的兜底**：若只有 GeoJSON，可做自动化"挤出→GLB→体素化"脚本（挤出逻辑可复用现有 `overlay.js` 的 `ExtrudeGeometry` 逻辑离线跑，见 5.1 的 P1），冻结为工具而非每次都手写。

***

## 4. D3 决策：CSS3D 交互标签

### 4.1 为什么现有叠加层不够

`overlay.js` 的叠加画布是 `position:fixed + pointer-events:none` 的透明 WebGL canvas——能画几何（挤出体、线框），但**不能承载 HTML 交互**（点击、悬停、富文本卡片），且鼠标事件全部透传给了 viewer。

### 4.2 三层渲染架构（从下到上）

```
┌─────────────────────────────────────────────┐
│ ③ CSS3D 层（新增）: <div> 容器 + CSS3DObject 标签  │  ← HTML/DOM，可交互
│   容器全部透传，仅标签元素 pointer-events:auto      │
├─────────────────────────────────────────────┤
│ ② three WebGL 叠加 canvas（现状 overlay.js）       │   ← 几何：GeoJSON 挤出体/线框/辅助线，pointer-events:none
├─────────────────────────────────────────────┤
│ ① viewer canvas（WebGPU / WebGL2 + 碰撞相机）       │   ← 高斯渲染 + 行走/飞行
└─────────────────────────────────────────────┘
        每帧：同一份 viewer 相机位姿/FOV 同步给 ② 与 ③
```

- ②③ 共用现有 `syncCamera()`（位置 + 四元数 + FOV 换算），CSS3D 层每帧 `updateMatrixWorld()` 即可。

- 标签世界坐标 = 数据坐标经同一变换（与挤出体一致：`(px, pz, -py)+基准高度`）；也可直接由独立标注 JSON（世界坐标）给出。

### 4.3 交互触发设计（前端需求级）

| 触发    | 行为                                                     |
| ----- | ------------------------------------------------------ |
| 悬停    | 高亮标签/显示小提示（旁路显示，不阻塞相机）                                 |
| 单击    | 弹出信息卡（HTML）：名称/说明/图片/跳转按钮                              |
| 双击/按钮 | 相机聚焦（将 viewer 相机飞行到标签点位，`CameraManager` 的 tween/flyTo） |
| 附加    | 标签按距离缩放/淡出；支持 `?labels=0` 一键隐藏                         |

数据来源建议：独立 `data/labels.json`（对标点位 + 文案 + 跳转），与 GeoJSON 解耦、不改 viewer 代码即可增删。

### 4.4 实现要点与风险

- **R1 构建：** 需把 `three/examples/jsm/renderers/CSS3DRenderer.js` 拷贝进 `dist/vendor/`；其源码为 `import 'three'` 裸模块名，需补 import map 或统一改为相对路径 `./vendor/three.module.js`（否则浏览器解析失败）。

- **R2 手势冲突：** walk/fly 模式为指针锁定/拖拽转头，DOM 标签点击可能与之冲突——提供"模式切换按钮"或锁定态下标签退化为"仅显示不可点击"；真机实测后定。

- **R3 遮挡策略：** 一期标签默认置顶（billboard），不做 splat 遮挡剔除（成本高）；如产品要求遮挡再评估离屏渲染方案。

- **兼容别称：** 官方 viewer 的 annotations 依赖场景格式（SOG），本项目走自研 CSS3D，**不受数据格式限制**——这也反过来支撑了 D1"不转 SOG"的结论。

***

## 5. 总体架构与实施计划

### 5.1 部署架构与目录约定

```
浏览器（PC/Android/鸿蒙/iOS 统一网页入口）
   │ HTTP(S), 静态文件 + Range
   ▼
静态服务器（本地 serve.mjs / nginx）
   ├─ dist/         viewer(index.html/index.js/css) + settings.json + overlay.js + labels.js + vendor/(three…)
   └─ data/（零拷贝映射）
        ├─ point_wlbwg.compressed.ply        # 主点云
        ├─ point_wlbwg.lite.compressed.ply   # [P3] 移动端降档
        ├─ lx.geojson                        # 套盒边界（可视化）
        ├─ xxx.voxel.json(+bin)              # 空气墙/碰撞 [P1]
        ├─ xxx.collision.glb                 # 碰撞可视化
        └─ labels.json                       # 交互标签 [P2]
```

### 5.2 实施步骤

| 阶段 | 内容                                                                                         | 交付物                                                                                           | <br />            |
| -- | ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- | :---------------- |
| P0 | 现状核对（已具备）：PLY 加载、GeoJSON 挤出、voxel 碰撞、相机同步                                                  | —                                                                                             | <br />            |
| P1 | 空气墙管线产品化：写 `src/geojson-to-mesh` 离线导出脚本（复用 Extrude 逻辑 → GLB）+ 体素化命令模板 + 对齐校验清单             | `scripts/build_collision.mjs`，一套盒一跑                                                           | <br />            |
| P2 | CSS3D 标签：vendor 拷贝与 import 修复 → `src/labels.js`（labels.json + DOM 气泡 + 相机聚焦）→ build.mjs 注入 | 交互原型：点击标签弹信息卡/聚焦                                                                              | <br />            |
| P3 | 跨端分档与真机矩阵：splat-transform 降采样 lite 档；\`?tier=pc                                            | mobile\` + UA 自动识别 + WebGL2 强制兜底；真机清单（Win Chrome/Edge、安卓 Chrome+WebView、鸿蒙 ArkWeb、iOS Safari） | 性能基线（加载时长/内存/FPS） |
| P4 | 真实套盒（新一套 PLY+GeoJSON）全流程联调                                                                 | 验收文档                                                                                          | <br />            |

***

## 6. 风险登记

| 风险                        | 影响         | 对策                                          |
| ------------------------- | ---------- | ------------------------------------------- |
| 9.12M splat 在移动端内存/GPU 超载 | 卡顿、白屏、崩溃   | 降档数据 + `?budget=` + WebGL2 + 低像素比；真机压测      |
| WebGPU 兼容差异               | PC 好、移动端不稳 | index.js 增加"不支持 WebGPU 自动回退 WebGL2"，不依赖人工带参 |
| GeoJSON 高程与点云不贴合          | 边界浮空/陷地    | 对齐校验 `?coldbg=1`；预留 `?polyy=` 高度偏移调参        |
| CSS3D 在各 WebView 渲染差异     | 标签漂移/抖动    | `will-change`、像素取整；真机矩阵回归                   |
| walk/fly 指针锁定与 DOM 点击冲突   | 标签点不了      | 模式切换/仅显示兜底（R2）                              |
| 147 MB 首包带宽               | 移动端首屏慢     | Range 流式 + 降档档位；如仍超标再评估 .spz                |
| SOG 机制随编辑器升级变动            | 版本兼容风险     | 不采用 SOG（D1），规避                              |

***

## 7. 未决问题（下一轮确认，便于出 v2）

- **U1 空气墙范围**：只要"场地边界禁出"，还是需要"室内房间/门洞级"？→ 决定走 2a 还是 2b。

- **U2 标签点位来源**：GeoJSON 属性、独立 `labels.json`、还是后台配置下发？

- **U3 分发形态**：纯静态网页直开，还是 iOS/鸿蒙/安卓以 WebView 壳接入？→ 影响真机清单与 WebGPU 策略验证。

- **U4 移动端目标档位**：降采样到 200 万还是 300 万 splat？

- **U5 坐标基准**：当前本地坐标 + 假经纬度（CRS84 占位）是否能长期维持？若需与地图联动的经纬度换算，需数据方补充转换公式。

